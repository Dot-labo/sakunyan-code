{
  "name": "@dotlabo/sakunyan-code",
  "version": "0.1.7",
  "description": "初心者向けのコーディングエージェント",
  "type": "module",
  "bin": {
    "sakunyan": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "devDependencies": {
    "@types/figlet": "^1.7.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@dotlabo/sakunyan-code": "^0.1.4",
    "@earendil-works/pi-coding-agent": "0.84.2",
    "@earendil-works/pi-tui": "0.84.2",
    "figlet": "^1.11.4"
  }
}
