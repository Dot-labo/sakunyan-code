## 1. 固定設定の一元管理と起動設定

- [x] 1.1 プロバイダ `openrouter` とモデル `deepseek/deepseek-v4-flash` を1つの設定モジュールで管理する。
- [x] 1.2 `src/cli.ts` からpiへ、設定モジュールの固定プロバイダとモデルを渡す。
- [x] 1.3 起動処理と接続確認が同じ設定モジュールを参照するようにする。
- [x] 1.4 通常のモデル／プロバイダ選択経路から固定設定を上書きできないことを確認する。

## 2. 起動時の設定フロー

- [x] 2.1 起動確認、先生へのAPIキー依頼、入力待ち、再試行失敗、設定成功の日本語メッセージを追加する。
- [x] 2.2 既存のTUI拡張ライフサイクルに、スピナー付きの起動時接続確認と確認結果表示を追加する。
- [x] 2.3 認証または接続に失敗した場合、接続確認NGを表示してAPIキー入力フローを開き、設定が成功またはキャンセルされるまで生徒の通常入力を受け付けない。
- [x] 2.4 送信されたキーをpi標準の認証ファイルへ先に反映して再読込し、固定モデルの確認へ使う。
- [x] 2.5 APIキーをログやエラー文に含めず、入力欄には表示し、キャンセルと明示的な終了を処理する。

## 3. 確認

- [x] 3.1 実APIを呼び出さずに、固定設定と設定状態遷移を確認するテストまたは実行可能なセルフチェックを追加する。
- [x] 3.2 `npm test` を実行し、TypeScriptのコンパイルを確認する。
- [x] 3.3 有効な先生提供のキーで手動起動し、通常利用の前にモデル応答を取得できることを確認する。
- [x] 3.4 キーなしまたは無効なキーで手動起動し、日本語の案内、入力待ち状態、再試行、APIキー表示を確認する。
