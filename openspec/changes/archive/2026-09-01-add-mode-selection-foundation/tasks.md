## 1. モード状態

- [x] 1.1 `src/modes.ts` に識別子、日本語を扱える表示名、説明、注意点、プロンプトを持つモード定義と既定モードIDを追加する
- [x] 1.2 既存の教育用プロンプトを既定モードの定義へ移し、CLIの固定プロンプト指定を外す
- [x] 1.3 現在モードを拡張内で保持し、セッション開始時に既定値へ戻す

## 2. 選択UI

- [x] 2.1 `/mode` を登録し、現在モードを初期選択として一覧・説明・注意点を表示する
- [x] 2.2 矢印キーによる候補移動、Enterによる確定、Esc・Ctrl+Cによる変更なしのキャンセルを実装する
- [x] 2.3 確定後に現在モードの表示を更新し、日本語の変更通知を表示する
- [x] 2.4 `before_agent_start` で現在モードのプロンプトを次のターンのシステムプロンプトへ追加する

## 3. フッター表示

- [x] 3.1 既存の状態表示更新を共通関数へまとめ、待機中と処理中のすべての状態行へ現在モード名を追加する

## 4. 検証

- [x] 4.1 モードIDの一意性、必須項目、既定ID、日本語の保持、既存教育用プロンプトの移行を自動テストする
- [x] 4.2 コマンド登録、一覧内容、候補移動、確定、キャンセル、セッション初期化、各処理状態でのモード表示を自動テストする
- [x] 4.3 確定したモードのプロンプトが次のターンへ適用され、キャンセル時は以前のプロンプトが維持されることを自動テストする
- [x] 4.4 モード変更がツール、権限、モデル、会話履歴へ影響しないことを既存テストと差分確認で検証する
- [x] 4.5 `npm run typecheck` と `npm test` を実行し、既存機能を含めて成功することを確認する
