## 1. APIキー状態の取得と表示用データ化

- [x] 1.1 OpenRouterキー情報APIのレスポンス型と表示用状態型を追加し、`limit`、`limit_remaining`、`expires_at` の存在・型・値域を検証する
- [x] 1.2 認証済みAPIキーを使って `GET https://openrouter.ai/api/v1/key` を呼び出す取得処理を追加し、タイムアウト・HTTPエラー・不正レスポンスを安全な取得失敗として扱う
- [x] 1.3 利用残量を0〜100%へ正規化し、固定幅のバーとパーセントへ変換する整形処理を追加する。上限なし、期限なし、取得失敗をそれぞれ区別する
- [x] 1.4 有効期限を残り時間（日・時間・分）だけへ簡潔に整形し、7日以内・期限切れ・通常の注意状態を判定する

## 2. TUIフッターへの統合

- [x] 2.1 フッター用の日本語ラベル、取得中、取得失敗、上限なし、期限なしのメッセージを `src/messages.ts` で一元管理する
- [x] 2.2 `src/extension.ts` の接続確認結果ウィジェットに、絵文字・有効期限の残り時間・残量バー・パーセントを同じ行で追加する
- [x] 2.3 接続確認結果ウィジェットの登録後にAPIキー状態を非同期取得し、状態更新ごとに表示を差し替える。取得失敗時は古い状態を残さず固定メッセージを表示する
- [x] 2.4 `ctx.modelRegistry.getApiKeyForProvider` から得たキーだけをHTTP認証に使い、APIキーやエラー詳細を表示状態・通知・ログへ渡さない
- [x] 2.5 セッション開始時、ターン終了時、約60秒間隔で状態を更新し、表示の破棄時にタイマーを解除する
- [x] 2.6 既存の日本語UIテーマを使って通常・期限間近・期限切れ・残量少の色を設定し、簡潔な1行のキー状態表示と既存の状態表示を維持する

## 3. テストと検証

- [x] 3.1 APIレスポンスの正常系、上限なし、欠落値、不正値、HTTPエラー、タイムアウトを実キーなしで検証するユニットテストを追加する
- [x] 3.2 残量割合、バー表示、残り時間、期限・残量の注意状態、情報取得不可メッセージの表示整形をテストする
- [x] 3.3 接続確認結果行の1行情報表示、既存のフッター状態表示、取得中から取得完了・失敗への更新、APIキー非表示を検証する
- [x] 3.4 `npm run typecheck` と `npm test` を実行し、既存の起動時APIキー設定および日本語UIのテストが回帰していないことを確認する
