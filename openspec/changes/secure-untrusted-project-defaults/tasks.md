## 1. pi起動設定を安全側へ固定する

- [ ] 1.1 `src/cli.ts`のpi起動引数へ`--no-approve`と`--no-context-files`を追加し、既存のプロジェクトリソース無効化引数とともに利用者引数より後で適用する
- [ ] 1.2 起動時のpiツール許可リストを`read,grep,find,ls`へ変更し、`bash`、`write`、`edit`をCLI引数だけで有効化できないようにする

## 2. アドバイスモードの対話方針を揃える

- [ ] 2.1 `src/educational-prompt.ts`から調査用コマンドを自動実行する指示を削除する
- [ ] 2.2 コマンドが必要な場合は、意味と手順を説明して子ども本人に実行してもらう指示を追加する

## 3. 安全な既定値をテストする

- [ ] 3.1 信頼拒否、コンテキストファイル無効化、プロジェクトリソース無効化の固定引数を確認する回帰テストを追加する
- [ ] 3.2 利用者が別の`--tools`を指定しても、起動時には`read,grep,find,ls`だけが適用されることを確認する回帰テストを追加する
- [ ] 3.3 教育プロンプトがコマンドの自動実行を許可せず、子ども本人へ実行を案内することを確認する回帰テストを追加する

## 4. 利用者向け説明と検証を完了する

- [ ] 4.1 READMEをアドバイスモードの読み取り専用4ツール、プロジェクト由来の命令を読み込まない動作、OSサンドボックスではない境界に合わせて更新する
- [ ] 4.2 型チェックと全テストを実行し、起動、APIキー設定、日本語UIの既存動作に回帰がないことを確認する
